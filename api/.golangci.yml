version: "2"

run:
  timeout: 5m

output:
  formats:
    colored-line-number: {}
  sort-results: true

formatters:
  enable:
    - gofumpt
    - goimports

linters:
  enable:
    - bodyclose
    - durationcheck
    - errcheck
    - copyloopvar
    - govet
    - nilerr
    - noctx
    - rowserrcheck
    - sqlclosecheck
    - staticcheck
    - errname
    - goconst
    - gocritic
    - misspell
    - nakedret
    - prealloc
    - predeclared
    - revive
    - unconvert
    - unparam
    - unused
    - whitespace
    - gosec
    - ineffassign
    - cyclop
    - gocognit
    - nestif
    - usetesting
    - tparallel

linters-settings:
  cyclop:
    max-complexity: 15
  gocognit:
    min-complexity: 20
  goconst:
    min-len: 3
    min-occurrences: 3
  errcheck:
    check-type-assertions: true
    check-blank: true
  govet:
    enable-all: true
  nakedret:
    max-func-lines: 10
  nestif:
    min-complexity: 5

formatters-settings:
  gofumpt:
    extra-rules: true

issues:
  max-issues-per-linter: 50
  max-same-issues: 5
  exclude-rules:
    - path: _test\.go
      linters:
        - goconst
        - gocritic
        - errcheck
        - gosec
